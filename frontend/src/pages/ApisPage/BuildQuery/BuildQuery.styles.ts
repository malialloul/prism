import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

// Content container for BuildQuery tab
export const ApisContent = styled(Box)({
  display: 'flex',
  flex: 1,
  gap: '1rem',
  minHeight: 0,
  overflow: 'hidden',
  width: '100%',
});
